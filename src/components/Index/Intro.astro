---
import P from "../../layouts/P.astro";
import Links from "./Links.astro";
---

<div
  data-scroll
  data-scroll-speed="1"
  class="bg-gray-900 h-full p-5 py-14 lg:pb-0 z-50 lg:bg-transparent bg-opacity-90 lg:w-3/4 flex-col z-1 flex lg:tracking-widest leading-loose"
>
  <h1
    class="font-bold w-full bg-gradient-to-br from-red-900 via-rose-900 to-violet-900 dark:from-purple-200 dark:via-green-300 dark:to-rose-200 bg-clip-text text-transparent flex gap-2 lg:gap-5 md:gap-4 md:flex-row md:items-end text-4xl md:text-8xl lg:text-8xl"
  >
    I'm <span
      class="underlined underline-clip bg-gradient-to-br from-red-900 via-rose-900 to-violet-900 dark:from-purple-200 dark:via-green-300 dark:to-rose-200 bg-clip-text"
      >{" "} Snehal,</span
    >
  </h1>
  <P>
    your friendly <span
      class="text-yellow-900 dark:text-yellow-300 transition duration-700 underlined underline-mask"
    >
      web developer</span
    >, sailing through the Grand Line of the Internet! With the powers bestowed
    upon me by the
    <acronym
      title="Computer Engineering "
      class="underline decoration-yellow-300 hover:bg-gradient-to-tr from-purple-400 to-rose-400 bg-clip-text hover:text-transparent font-extrabold transition duration-300 ease-in-out origin-left"
    >
      Comp-Engu</acronym
    >
    no Mi devil fruit
    <!-- <img src="/fruit.png" width="50" height="50" alt="fruit" class="inline"> -->
    and the Ingenius Haki, I am passionate about creating captivating websites and
    building cool web applications.
  </P>
  <Links />
</div>

<style>
  .name:hover {
    font-variation-settings: "wght" 582;
    letter-spacing: 6px;
  }

  .underlined {
    /*   background: red; */
    position: relative;
  }

  .underline-mask:after {
    content: "";
    position: absolute;
    top: 70%;
    width: 150%;
    aspect-ratio: 4 / 1;
    left: 50%;
    transform: translate(-50%, 0);
    border-radius: 50%;
    border: 6px solid hsl(280 80% 50%);
    /* Use a conic gradient mask to hide and show the bits you want */
    --spread: 140deg;
    --start: 290deg;
    mask: conic-gradient(
      from var(--start),
      white 0 var(--spread),
      transparent var(--spread)
    );
  }

  .underline-overflow {
    display: inline-block;
    /* A little extra padding and overflow hidden   */
    overflow: hidden;
    padding-bottom: clamp(1rem, 2vmin, 2rem);
  }
  .underline-overflow:after {
    content: "";
    position: absolute;
    top: 100%;
    height: 150%;
    aspect-ratio: 5 / 0.5;
    left: 50%;
    transform: translate(-50%, -10%);
    border-radius: 50%;
    border: 6px solid hsl(10 80% 50%);
  }

  .underline-clip:after {
    content: "";
    position: absolute;
    top: 90%;
    width: 150%;
    aspect-ratio: 5/0.5;
    left: 50%;
    transform: translate(-50%, 0);
    border-radius: 50%;
    border: 6px solid hsl(130 80% 50%);
    /* Use a clip-path to hide and show the bits you want */
    clip-path: polygon(0 0, 50% 50%, 100% 0);
  }

  .bg {
    background: url("../../assets/bg.svg");
    background-size: cover;
  }
</style>
